{
  "name": "referal",
  "version": "1.0.0",
  "description": "Referal program",
  "main": "index.js",
  "engines": {
    "node": "v16.15.0"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shengkai.kong",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "pg": "^8.7.3",
    "uuid": "^8.3.2"
  }
}
